export type collection = {
  name: string;
  collectionId: string;
  prompt: string;
};

export type collections = {
  name: string;
  collectionId: string;
  prompt: string;
}[];

export type document = {
  name: string;
  id: string;
};

export type documents = {
  name: string;
  id: string;
}[];
